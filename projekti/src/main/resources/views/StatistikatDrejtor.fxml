<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.chart.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>

<AnchorPane xmlns="http://javafx.com/javafx/23.0.1"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="controllers.StatistikatDrejtorController"
            prefWidth="1000" prefHeight="600">

    <children>

        <!-- Titulli -->
        <Label layoutX="30" layoutY="20"
               text="📊 Paneli i Statistikave të Shkollës"
               style="-fx-font-size: 22px; -fx-font-weight: bold;" />

        <!-- Butoni Rifresko -->
        <Button fx:id="btnRifresko" text="🔄 Rifresko"
                layoutX="820" layoutY="20"
                prefWidth="120" prefHeight="30" />

        <!-- Kornizë për grafikët -->
        <AnchorPane layoutX="30" layoutY="60" prefWidth="940" prefHeight="500"
                    style="-fx-border-color: #2a9d8f;
                           -fx-border-width: 3px;
                           -fx-border-radius: 20px;
                           -fx-background-radius: 20px;
                           -fx-background-color: #f0f0f0;">
            <children>

                <!-- BarChart majtas -->
                <BarChart fx:id="barChart"
                          layoutX="20" layoutY="20"
                          prefWidth="440" prefHeight="440">
                    <xAxis>
                        <CategoryAxis fx:id="xAxis" label="Kategoria" />
                    </xAxis>
                    <yAxis>
                        <NumberAxis fx:id="yAxis" label="Vlera" />
                    </yAxis>
                </BarChart>

                <!-- PieChart i jashtëm (i madh) -->
                <PieChart fx:id="outerPieChart"
                          layoutX="520" layoutY="50"
                          prefWidth="350" prefHeight="350"
                          style="-fx-background-color: transparent;" />

                <!-- PieChart i brendshëm (simulon vrimën e donut) -->
                <PieChart fx:id="innerPieChart"
                          layoutX="595" layoutY="125"
                          prefWidth="200" prefHeight="200"
                          style="-fx-background-color: transparent;" />

            </children>
        </AnchorPane>
    </children>
</AnchorPane>
